server:
  port: 8082

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5433/fishwish
    username: fishwish
    password: fishwish123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Configuración para la comunicación entre servicios
app:
  product-service:
    url: http://localhost:8081/api/products
